#include<conio.h>
#include<stdio.h>
voiod main()
{
clrscr();
int i, number, compare;
printf("Enter a number");
scanf("%d", &number);
for(i=1,i<=n,i++)
{
	if(number%i==0)
	{
		compare=compare+1;
	}
}
	if(c==2)
	printf("Number is Prime number");
	else
	printf("Number is not prime" );
	getch();
}

